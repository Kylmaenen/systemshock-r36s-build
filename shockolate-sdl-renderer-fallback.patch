--- a/src/MacSrc/Shock.c
+++ b/src/MacSrc/Shock.c
@@ -28,6 +28,7 @@
 //  Includes
 //--------------------
 #include <math.h>
+#include <stdlib.h>
 #include <SDL.h>
 
 #include "InitMac.h"
@@ -61,6 +62,8 @@
 int num_args;
 char **arg_values;
 
+static const char *gRequestedSDLRenderDriver = "opengl";
+
 extern grs_screen *svga_screen;
 extern frc *svga_render_context;
 
@@ -76,6 +79,44 @@
 extern void CreateDefaultKeybindsFile(void);
 extern void LoadHotkeyKeybinds(void);
 extern void LoadMoveKeybinds(void);
+
+static const char *GetSDLRenderDriverSetting(void) {
+    const char *driver = SDL_getenv("SHOCKOLATE_RENDER_DRIVER");
+
+    if (driver == NULL || driver[0] == '\0')
+        driver = SDL_getenv("SDL_RENDER_DRIVER");
+
+    if (driver == NULL || driver[0] == '\0')
+        driver = "opengl";
+
+    return driver;
+}
+
+static SDL_Renderer *CreateShockRenderer(SDL_Window *target_window, const char *driver) {
+    SDL_Renderer *new_renderer;
+
+    if (driver != NULL && driver[0] != '\0')
+        SDL_SetHint(SDL_HINT_RENDER_DRIVER, driver);
+
+    new_renderer = SDL_CreateRenderer(target_window, -1, SDL_RENDERER_PRESENTVSYNC);
+    if (new_renderer != NULL)
+        return new_renderer;
+
+    WARN("SDL_CreateRenderer failed for driver '%s' with vsync: %s", driver, SDL_GetError());
+
+    new_renderer = SDL_CreateRenderer(target_window, -1, 0);
+    if (new_renderer != NULL)
+        return new_renderer;
+
+    WARN("SDL_CreateRenderer failed for driver '%s': %s", driver, SDL_GetError());
+
+    SDL_SetHint(SDL_HINT_RENDER_DRIVER, "software");
+    new_renderer = SDL_CreateRenderer(target_window, -1, 0);
+    if (new_renderer == NULL)
+        WARN("SDL_CreateRenderer failed for fallback driver 'software': %s", SDL_GetError());
+
+    return new_renderer;
+}
 
 //------------------------------------------------------------------------------------
 //		Main function.
@@ -165,8 +206,10 @@
 }
 
 void InitSDL() {
+    gRequestedSDLRenderDriver = GetSDLRenderDriverSetting();
+
     SDL_SetHint(SDL_HINT_NO_SIGNAL_HANDLERS, "1");
-    SDL_SetHint(SDL_HINT_RENDER_DRIVER, "opengl");
+    SDL_SetHint(SDL_HINT_RENDER_DRIVER, gRequestedSDLRenderDriver);
     if (SDL_Init(SDL_INIT_VIDEO | SDL_INIT_TIMER | SDL_INIT_AUDIO) < 0) {
         DEBUG("%s: Init failed", __FUNCTION__);
     }
@@ -201,6 +244,13 @@
 
     window = SDL_CreateWindow(window_title, SDL_WINDOWPOS_CENTERED, SDL_WINDOWPOS_CENTERED, grd_cap->w, grd_cap->h,
                               SDL_WINDOW_HIDDEN | SDL_WINDOW_RESIZABLE | SDL_WINDOW_ALLOW_HIGHDPI | SDL_WINDOW_OPENGL);
+    if (window == NULL) {
+        ERROR("SDL_CreateWindow failed: %s", SDL_GetError());
+        exit(1);
+    }
+
+    INFO("SDL video driver: %s", SDL_GetCurrentVideoDriver());
+    INFO("Requested SDL render driver: %s", gRequestedSDLRenderDriver);
 
     // Create the palette
 
@@ -219,8 +269,19 @@
 
     SDL_RaiseWindow(window);
 
-    renderer = SDL_CreateRenderer(window, -1, SDL_RENDERER_PRESENTVSYNC);
-    SDL_RenderSetLogicalSize(renderer, grd_cap->w, grd_cap->h);
+    renderer = CreateShockRenderer(window, gRequestedSDLRenderDriver);
+
+    if (renderer == NULL) {
+        ERROR("SDL_CreateRenderer failed: %s", SDL_GetError());
+        exit(1);
+    }
+
+    SDL_RendererInfo renderer_info;
+    if (SDL_GetRendererInfo(renderer, &renderer_info) == 0)
+        INFO("SDL renderer: %s", renderer_info.name);
+
+    if (SDL_RenderSetLogicalSize(renderer, grd_cap->w, grd_cap->h) < 0)
+        WARN("SDL_RenderSetLogicalSize failed: %s", SDL_GetError());
 
     // Startup OpenGL
 

