--- a/src/MusicSrc/MusicDevice.c
+++ b/src/MusicSrc/MusicDevice.c
@@ -194,7 +194,9 @@ static int AdlMidiInit(MusicDevice *dev, const unsigned int outputIndex, unsigned samplerate)
     adl_switchEmulator(adl, ADLMIDI_EMU_DOSBOX);
     adl_setNumChips(adl, 1);
     adl_setVolumeRangeModel(adl, ADLMIDI_VolumeModel_AUTO);
-    adl_setRunAtPcmRate(adl, 1);
+    // RK3326/dArkOSRE stutters when ADLMIDI does OPL work at full PCM cadence.
+    // Keep Sound Blaster ADLMIDI music, but let libADLMIDI run its lighter timing path.
+    adl_setRunAtPcmRate(adl, 0);
 
     adev->adl = adl;
 
